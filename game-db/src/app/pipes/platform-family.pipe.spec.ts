import { PlatformFamilyPipe } from './platform-family.pipe';

describe('PlatformFamilyPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformFamilyPipe();
    expect(pipe).toBeTruthy();
  });
});
